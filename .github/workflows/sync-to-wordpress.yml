name: Sync to WordPress (DISABLED)

# DISABLED: 一括投稿問題により無効化
# on:
#   push:
#     branches: [ main ]
#     paths: [ 'articles/**' ]
#   workflow_dispatch:

# 手動実行のみ許可
on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    
    steps:
    - name: Safety Check
      run: |
        echo "⚠️ このワークフローは安全性確認のため無効化されています"
        echo "手動で安全な同期スクリプトを使用してください"
        exit 1
